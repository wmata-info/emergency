function alertE(){alert("Your connection is End-to-end encrypted with our origin servers.\n\nWhen something is encrypted end-to-end with origin servers, it means that the information gets shrouded into a secret code right from where it starts, either on the client-side or server-side, and it stays that way until it reaches its destination. This keeps the information safe and ensures only the right person can unlock and understand it and away from prying eyes.")}